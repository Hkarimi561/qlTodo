/**
 * Fill these in with your own Supabase project's values (Project Settings ->
 * API in the Supabase dashboard). The anon/public key is safe to ship in a
 * browser bundle — Row Level Security policies decide what it can actually
 * do (see the setup SQL in README.md).
 */
export const environment = {
  supabaseUrl: 'https://YOUR-PROJECT.supabase.co',
  supabaseAnonKey: 'YOUR-ANON-PUBLIC-KEY',
};
